# Local Fonts

Fonts in this directory are generated by running:

```bash
pnpm fonts:prepare
```

The command copies Inter (Latin) and Noto Sans JP (Japanese) variable fonts from the installed `@fontsource-variable/*` packages so they can be preloaded by Astro.

